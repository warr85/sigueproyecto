{% extends 'base.html.twig' %}

{% block body %}
    <h1>Persona creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}

                {{ form_widget(formEconomico) }}

        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('admin_persona_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
